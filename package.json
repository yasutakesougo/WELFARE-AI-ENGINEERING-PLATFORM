{
  "name": "waep-learning-slice-a",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build:risk-detector": "tsc -p tsconfig.risk-detector.json",
    "risk:classify": "npm run build:risk-detector --silent && node .waep-build/risk-detector/risk_detector/cli.js"
  },
  "devDependencies": {
    "@types/node": "22.18.0",
    "typescript": "5.9.2",
    "vitest": "3.2.4"
  }
}
